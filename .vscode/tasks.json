{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build service",
            "type": "shell",
            "command": "go build -o koiot",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "cmd"
            },
            "problemMatcher": [
                "$go"
            ]
        }
    ]
}
